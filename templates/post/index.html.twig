{% extends 'base.html.twig' %}

{% block title %}Blog{% endblock %}
{% block stylesheets %}
<link rel="stylesheet" href="{{ asset('css/post/style.css') }}">
{% endblock %}
{% block body %}

<div class="main-container">
    <h1 class="text-center">Blog</h1>

    <div class="section-blog">
        <div class="row">
            {% for post in posts %}
                <div class="col-4 mt-3 mb-5">
                    <div class="card w-90">
                        <img class="card-img-top" src={{post.image}} alt="Card image cap" height="200px">
                        <div class="card-body">
                            <h6 class="card-subtitle mb-2 text-muted">Business, Travel <span class="text-blue">- {{post.createdAt|date('M d, Y H:i:s')}}</span></h6>
                            <h5 class="card-title">{{post.title}}</h5>
                            <p class="card-text mb-0">{{post.content|slice(0, 100)}}...</p>
                            <a href="/blog/{{post.id}}" class="card-link">More...</a>
                            <div class="row mt-2">
                                <div class="col-2">
                                    {% if post.author.logo == null %}
                                        <img src="{{ asset('img/profil/01.png') }}" alt="profil" width="50px" width="50px">
                                    {% else %}
                                        <img src="{{post.author.logo}}" alt="profil" width="50px" width="50px">
                                    {% endif %}
                                </div>
                                <div class="col-10">
                                    <div>{{post.author.firstname}}</div>
                                    <div>{{post.author.lastname}}</div>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            {% endfor %}
        </div>
    </div>
</div>
{% endblock %}
